'use strict';
// const { NUMBER } = require('../core/enum');
module.exports = () => {
//   return async function log(ctx, next) {
//     if (ctx.request.url.indexOf('public') !== NUMBER.ONE) {
//       await next();
//       return;
//     }
//     const token = ctx.cookies.get('member_token', {
//       // signed: true,
//       // encrypt: true,
//     });
//     if (!token) {
//       await next();
//       return;
//     }
//     const memberId = await ctx.app.cache.get(token);
//     if (memberId) {
//       ctx.memberId = memberId;
//     }
//     await next();
//   };
};

